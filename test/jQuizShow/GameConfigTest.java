
package jQuizShow;

import static junit.framework.Assert.assertEquals;
import junit.framework.TestCase;


public class GameConfigTest extends TestCase {
    
    public GameConfigTest() {
        super(null);
    }
    
public void testGetInstance() 
{
  // GameConfig instance= new GameConfig();
  // instance.getInstance();
}
   
public void testGetNumLevels() 
{
  // GameConfig instance= new GameConfig();
  // instance.getNumLevels();
}

public void testGetLevelName() 
{
    
  //  GameConfig instance= new GameConfig();
  //test case 1 -- if number of level < 0
  //  assertEquals("Error",instance.getLevelName(-1));
    // suppose we have zero levels
  // instance.getLevelName(0);
}

}
